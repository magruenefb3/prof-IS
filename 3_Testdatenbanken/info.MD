# MySQL-Datenbank für das Professoren-Informationssystem (prof-IS)

Die MySQL-Datenbank wurde mit folgendem Befehl erzeugt:
`mysqldump -u root -p --add-drop-database --databases profis > create_profis_db.sql`

